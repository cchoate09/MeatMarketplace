# Sandbox Workspace

The mobile app now lives in `apps/farm-meat-marketplace`.

Quick start:

```powershell
cd apps\farm-meat-marketplace
.\.tools\node\npm.cmd run typecheck
.\.tools\node\npm.cmd run start
```
